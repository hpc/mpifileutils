INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

LIST(APPEND dtar_srcs
  dtar.c
  mfu_flist_archive.c
  )

BLT_ADD_EXECUTABLE(NAME dtar
  SOURCES ${dtar_srcs}
  DEPENDS_ON mfu LibArchive)
SET_TARGET_PROPERTIES(dtar PROPERTIES C_STANDARD 99)
INSTALL(TARGETS dtar RUNTIME DESTINATION ${X_BINDIR})
